from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Demo on how we can use data versioning with pipelines',
    author='Pratik M',
    license='MIT',
)
